import React from 'react';
import PropTypes from 'prop-types';

function Nav (props) {
    return (
        <div className="Nav">
            
        </div>
    );
}
  
Nav.propTypes = {
    name: PropTypes.string.isRequired
};
  
export default Nav;